import React from 'react';

const Button = () => {
  return (
    <input
      type="submit"
      value="Submit"
      name="submit"
      className="bg-white pa3 hover grow pointer"
    />
  );
};

export default Button;
